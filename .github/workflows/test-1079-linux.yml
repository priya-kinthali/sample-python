name: Test 1079

on:
  push:
    branches:
      - test-973
  workflow_dispatch:

jobs:
  test-python-executables:
    strategy:
      matrix:
        os: [ubuntu-24.04]
        # architecture: [x86, x64]

      fail-fast: false

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: |
            3.8
            3.9
            3.10
          # architecture: ${{ matrix.architecture }}
      # - name: Check for Python 3.8 files (Linux)
      #   shell: bash
      #   run: |
      #     echo "Checking Python 3.8 files on Linux..."
      #     find /opt/hostedtoolcache/Python/3.8 -name "python3.8.10*" -type f
      # - name: Check for Python 3.9 files (Linux)
      #   shell: bash
      #   run: |
      #     echo "Checking Python 3.9 files on Linux..."
      #     find /opt/hostedtoolcache/Python/3.9 -name "python3.9*" -type f